<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.skytech.ssm.dao.IUserDao">
    <select id="queryForUser" resultType="com.skytech.ssm.entity.User">
		SELECT * FROM USER ORDER BY SEX
 	</select>
</mapper>